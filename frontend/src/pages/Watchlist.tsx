import { FC } from "react";
import { Box, Typography } from "@mui/material";

const Watchlist: FC = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" mb={4}>
        Watchlist
      </Typography>
      <Typography>Watchlist page coming soon...</Typography>
    </Box>
  );
};

export default Watchlist;
