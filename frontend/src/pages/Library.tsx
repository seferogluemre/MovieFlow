import { FC } from "react";
import { Box, Typography } from "@mui/material";

const Library: FC = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" mb={4}>
        Library
      </Typography>
      <Typography>Library page coming soon...</Typography>
    </Box>
  );
};

export default Library;
