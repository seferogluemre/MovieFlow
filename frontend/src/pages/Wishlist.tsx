import { FC } from "react";
import { Box, Typography } from "@mui/material";

const Wishlist: FC = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" mb={4}>
        Wishlist
      </Typography>
      <Typography>Wishlist page coming soon...</Typography>
    </Box>
  );
};

export default Wishlist;
